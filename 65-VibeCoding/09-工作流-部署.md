# 部署

```bash

# 安装
apt install nginx

# 参考由于服务是把8080端口暴露给用户，所以修改相关配置把对应的80改成8080
略

# 重启服务
sudo service nginx restart
sudo service nginx reload

# 这会让 nginx 在前台运行，适合 Docker。
nginx -g "daemon off;"

```
